# name: MLOps CI/CD Pipeline

# on:
#   push:
#     branches: [ "main" ]
#   pull_request:
#     branches: [ "main" ]

# jobs:
#   # JOB 1: Test the Backend
#   test-server:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'
#       - name: Install Dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt
#       - name: Lint with flake8
#         run: pip install flake8 && flake8 server/

#   # JOB 2: Test the Frontend
#   test-client:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '22'
#       - name: Install & Build
#         run: |
#           cd client
#           npm install
#           npm run build

#   # JOB 3: Build & Push Docker Images
#   build-and-push:
#     needs: [test-server, test-client] # Only run if tests pass
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
      
#       - name: Log in to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       - name: Build and Push Server
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           file: ./Dockerfile.server
#           push: true
#           tags: ${{ secrets.DOCKERHUB_USERNAME }}/mlops-server:latest

#       - name: Build and Push Client
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           file: ./Dockerfile.client
#           push: true
#           tags: ${{ secrets.DOCKERHUB_USERNAME }}/mlops-client:latest